# Generality axes

This is a **typed semantic backlog**, not a list of implemented kernels. A descriptor and independent references may be added before a native path. A new physical backend branch must pass the [charter's admission rule](compiler-charter.md#backend-branch-admission). The [architecture ledger](../planning/architecture-composition.md) maps all 76 mixer-relevant source rows to external calls and unresolved axes.

| Axis | Closed semantic fields required | Independent clients / decisive gate |
|---|---|---|
| **A1: slot/channel** | Logical slot, key-channel and value domains; gate broadcast; per-domain state layout | GLA/KDA/Rodimus; preserve gate gradients and state shape |
| **A2: locality / indexed K1** | Exact or approximate metric, route ties, capacity, geometric metadata; the indexed gather-attend schedule | Foveal plus synthetic indexed K1; include route work and approximation quality separately. **Admitted (partial):** `K1Descriptor.indexed` + the `gather_indices` operand — gather the external per-query source set, attend over the gathered K/V (GQA-aware). Verified clients: NSA selected branch (005), Longformer band∪global (071), MoBA top-k blocks (006), DSA lightning indexer top-k (007), Sparse Transformer static patterns (072). The routes stay external. External residual: Foveal (065, needs the polar reducer — a single-client A13 sub-law) |
| **A3: coordinated routing** | Assignment, ownership, collision/merge, deterministic tie and return protocol | MoM and multi-head synthetic graph; conserve tokens/state and cost packing |
| **A4: hierarchical chunks** | Architectural pooling versus schedule subdivisions, level identity and boundary state | Log-linear and CAT; no schedule-only substitution for model compression. **Admitted (reference-tier):** the typed `DyadicBankedState` op — a bank of K2-matrix states with the carry-cascade promote/reset lifecycle (decay-forward form) — reproduces the pinned naive across shapes incl. partial dyadic blocks. Log-linear (009) and LogLinearMamba2 (046) both route through it (they share the mixer; only the frontend differs). The disjoint dyadic-block decomposition (`log_linear_disjoint_reference`) is retained as a comparator. **Reference-tier only:** the shared mixer is one structural family, so no native branch per the two-client gate; the pinned chunked *streaming* kernel (partial-chunk carry) is the residual native schedule; CAT (066) is the broader A4 candidate that would justify a native branch. See the [integration review](../planning/architecture-urm-integration.md). |
| **A5: timescale banks** | Independent state instances, decay schedules and explicit weighted combination | RetNet and multi-state synthetic graph; both state VJPs |
| **A6: slot interaction graph** | Edge domain, ordered propagation and state effects | Graph-memory candidates; derive graph update before admission |
| **A7: adaptive cardinality** | Allocation, birth/death, identity, reset and ragged cache ABI | CAT/dynamic memory; capacity and gradient policy |
| **A8: read/write coupling** | Independent factors, low-rank rank, augmented states, input-precomputable versus state-dependent coefficients | Generalized delta/RWKV-7/momentum; bound rank growth and prove VJP. **Typed IR admitted (partial):** generalized rank-1 K2 transition (`LinearDeltaSpec` erase_gate/write_gate/predict_key/low_rank) and ordered multi-delta (`num_deltas`) for GDN2 (027), RWKV-7/DPLR (042/032), IPLR (031), Comba (037), Gated DeltaProduct (029). **Mamba-2 (044) routes through public K2:** its scalar-per-head transition is exactly the canonical additive law (`gate_scope=head`, `delta=False`, state `M[dstate, head_dim]`), verified against the pinned chunked SSD ref (1.8e-7). **Mamba-1 (043) routes through public K2:** its full per-element gate `exp(dt_d·A_{d,n})` is the new `K2GateScope.ELEMENTWISE` reference-tier law (H=D channels, K=1 degenerate key/query, V=N state), verified against the diagonal scan at 0.0. Reference-tier only — the elementwise gate is single-client (HGRN's vector-state channel gate is adjacent but not a structural match), so no native branch per the two-client rule. Momentum DeltaNet (030) and Gated Oja (033) remain external. See the [integration review](../planning/architecture-urm-integration.md). |
| **A9: complex/block-real state** | Rotation representation, conjugate rules, phase state and VJP | Phase-bearing SSMs; exact block-real parity |
| **A10: stochastic routes** | RNG state, replay, estimator and distribution | Sampled routing; replay and estimator tests |
| **A11: parameter/expert/depth** | Static parameter domain, depth dependencies, grouped GEMM/dispatch and gradient accumulation | PAttention/AttnRes; complete replaced-layer parity |
| **A12: inner optimization** | Closed loss/update/optimizer state, loop bounds, checkpoint and outer-gradient policy | Titans/TTT; no callback or affine-scan assertion |
| **A13: score/reduction algebra** | Score map, normalization, neutral element, all-masked behavior, sum/LSE/max/positive or iterative reduction and VJP | POLAR/Foveal, TDA/KATA as distinct equations; physical reuse must be measured. **Admitted (partial):** `K1ScoreLaw` (DOT + CHANNEL_DECAY) and `K1ReducerLaw` (SOFTMAX + THRESHOLD_RELU_POWER + SQUARED_SUM + MAP_NORMALIZE) in the K1 descriptor — verified clients FoX (additive bias via `score_bias`), Wall (channel-decay), TDA (threshold), KATA (squared-sum), PAttention (map_normalize: score_map EXP/GELU × Lp `normalizer_p` × map↔norm order, reference-tier). The native/SDPA/oracle providers decline non-softmax reducers; only the Torch reference executes them. External residual: Parallax (multi-statistic), POLAR/Foveal (polar/null-sink) |
| **A14: cross-call composition** | Typed producer/consumer edges, output pairing, coefficients, state ownership and legal fusion | Differential/TDA/HLA. **HLA Nest composes through the public graph:** two dependent additive K2 calls (S_t += k kᵀ, r_t = S_t q_t; F_t += r vᵀ, o_t = q_tᵀ F_t) with explicit S/F state edges and the intermediate r materialized — verified against the serial Algorithm 1 recurrence and the closed-form masked identity; unfused (fusion a later proven rewrite). |
| **UT: causal triangular transform** | Strict-past data-dependent operand correction (Householder/triangular solve), the strict/inclusive diagonal split and its VJP — NOT a per-token projection | PaTH (010) and DeltaFormer (013). **Admitted (reference-tier):** the typed `TriangularSolve` ordinary operator (strict-causal forward-substitution correction `u = (I + diag(β)·strict_tril(P))^{-1}·v`) — the read-dependent write the canonical K2 rank-1 law cannot express. Both clients route through it: DeltaFormer (013) stage-1 value correction (both stages public: solve + K1), and PaTH (010) single-chunk — its Householder transform `inv(I + strict_tril(w_β wᵀ))` is exactly `TriangularSolve` with `P = w wᵀ` (Gram), verified 3e-8, with the additive Householder score correction + cumulative-gate bias riding the public K1 `score_bias`. This is the **second structural client** for `TriangularSolve`. **Residual (not claimed):** PaTH's multi-chunk progressive cross-chunk correction (a blockwise right-to-left score-transform scan with no typed K1 interface); a native blockwise solve schedule. See the [integration review](../planning/architecture-urm-integration.md). |

## Current execution boundary

At HEAD after the corrective integration (PAttention / HLA / Mamba-2 landed): 14 K1 dense-softmax fragments, one K3 route-to-state fragment and the K2 `linear_delta_state` fragments execute through the public graph path on the reference tier, **plus** PAttention (K1 `map_normalize` reducer, reference-tier), HLA (a two-node public K2 Nest graph) and Mamba-2's recurrent core (public head-gated matrix K2). The provider-decline contract is enforced: the native Triton, SDPA and NumPy-oracle K1 providers decline the score/reducer laws and the indexed path they do not execute (previously they accepted and silently ran softmax). The external Mamba-1 diagonal scan, the A4 hierarchical (log-linear) law, the UT triangular transforms (DeltaFormer/PaTH) and H3/Hyena remain external references without a public descriptor or native schedule. See the [integration review](../planning/architecture-urm-integration.md) and [evidence rules](../validation/evidence.md).

For every axis, record: descriptor and serialization; rejected combinations; NumPy/Torch references; two independent client graphs; source equations and proof obligations; provider capability; native forward/backward/decode envelope; end-to-end cost. A family label or axis label alone grants none of those stages.

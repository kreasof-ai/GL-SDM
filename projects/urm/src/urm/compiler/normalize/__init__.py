"""Graph normalization: builders that construct typed IR from frontend input.

Owns the normalization of user-facing specs and recipe documents into the
typed semantic program (:mod:`urm.ir.program`).
"""

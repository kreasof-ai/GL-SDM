"""Optional feasibility/optimization solvers.

Owns the generic constraint vocabulary (:mod:`urm.compiler.solve.constraints`)
and the optional Z3 backend (:mod:`urm.compiler.solve.z3`). Solver results are
always checked by independent verification.
"""

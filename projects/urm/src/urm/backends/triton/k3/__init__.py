"""Native K3 Triton kernels: selected state reads/writes.

Contains the route-selection and state read/update/backward kernels with narrow
launchers. Route/state composition and schedule belong to the compiler.
"""

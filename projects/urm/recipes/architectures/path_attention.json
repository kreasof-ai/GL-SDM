{
  "schema_version": 1,
  "name": "path_attention",
  "kind": "complete_model_graph",
  "source": {
    "comparator": "benchmarks.comparators.fla_k2",
    "notes": "arch-010 (UT causal triangular transform). PaTH single-chunk factors into PUBLIC ops: the Householder transform T_mat = inv(I + strict_tril(w_β wᵀ)) is exactly the public triangular_solve op (P = w wᵀ Gram, beta, identity value columns — verified 3e-8), and the causal softmax @ v with the additive score bias (the Householder score correction A_local − tril(qkᵀ) = −tril(qwᵀ)@(T_mat@tril(w_βkᵀ)), plus the FoX cumulative-gate bias scale·(gc_i − gc_j)) is the public K1 call. Verified against fla/ops/path_attn/naive.py @ 864a87f6 (single-chunk, 2.4e-7). Layer module: architectures/path_attention.py."
  },
  "layers": [{"id": "path_layer", "operation": "triangular_solve", "external_component": "architectures.path_attention.PaTHAttentionLayer", "params": {"axis": "UT: Householder via the PUBLIC triangular_solve op (P=wwᵀ) + PUBLIC K1 with additive Householder-correction + cumulative-gate score bias", "external_stages": ["short-conv/l2-norm on w frontend", "Householder score-correction assembly (−tril(qwᵀ)@Twbk)", "FoX cumulative-gate cumsum (gc_i − gc_j)", "multi-chunk progressive correction"], "note": "single-chunk public path verified; multi-chunk progressive path residual"}, "state": {}, "cache": {}}],
  "coverage": {"level": "generic_model_integration", "validated": true, "notes": "Single-chunk parity vs the pinned naive_path_attn (fp32, 2.4e-7); the public triangular_solve Householder transform vs the serial comparator (3e-8); the Householder-correction-active invariant; and gradients to q and w — in tests/test_architectures_ut.py. The MULTI-CHUNK progressive cross-chunk correction (the blockwise right-to-left score-transform scan) is the residual PaTH-specific schedule, recorded not claimed; the w frontend and chunked kernel are external."}
}

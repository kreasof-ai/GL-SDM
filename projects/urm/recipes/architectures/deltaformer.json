{
  "schema_version": 1,
  "name": "deltaformer",
  "kind": "complete_model_graph",
  "source": {
    "comparator": "benchmarks.comparators.fla_k2",
    "notes": "arch-013 (UT causal triangular transform). DeltaFormer: stage 1 strict-causal triangular value correction u = (I + diag(β)·strict_tril(P))^{-1}·v by forward substitution (P = tril_softmax(qkᵀ/√D, strict)) — now the typed public `triangular_solve` op (the strict-past correction is a read-dependent write the canonical K2 rank-1 law cannot express; verified numerically) — and stage 2 ordinary causal softmax attention over u via the typed K1 call. Verified against fla/ops/deltaformer/naive.py @ 864a87f6. Layer module: architectures/deltaformer.py."
  },
  "layers": [{"id": "deltaformer_layer", "operation": "triangular_solve", "external_component": "architectures.deltaformer.DeltaFormerLayer", "params": {"axis": "UT: strict-causal triangular value correction (typed public triangular_solve op) + K1 over corrected values", "external_stages": ["strict-lower softmax P (tril_softmax strict)", "β frontend", "chunked/kernel forms"], "note": "stage 1 strict-causal public op, stage 2 K1 includes diagonal"}, "state": {}, "cache": {}}],
  "coverage": {"level": "generic_model_integration", "validated": true, "notes": "Full parity vs the pinned naive_deltaformer_attn_head_first (fp32, 1.2e-7); the public triangular_solve op matches the independent serial forward-substitution comparator exactly (0.0); the strict-past invariant (future-value perturbation leaves earlier outputs unchanged); and gradients — in tests/test_architectures_ut.py. The strict-lower P frontend and the chunked/kernel forms are residual, not claimed."}
}

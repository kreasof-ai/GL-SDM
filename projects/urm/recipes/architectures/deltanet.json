{
  "schema_version": 1,
  "name": "deltanet",
  "kind": "complete_model_graph",
  "source": {
    "comparator": "benchmarks.comparators.fla_k2",
    "notes": "arch-025. Exact U2.D (c=1, beta, G=I, read after update) — verified in the sweep against fla/ops/delta_rule/naive.py + fla/layers/delta_net.py @ 864a87f6. External stages: Q/K/V projections, l2 q/k normalization, sigmoid beta projection, output RMSNorm and output projection. Layer module: architectures/deltanet.py."
  },
  "layers": [
    {
      "id": "deltanet_layer",
      "operation": "linear_delta_state",
      "external_component": "architectures.deltanet.DeltaNetLayer",
      "params": {
        "external_stages": [
          "Q/K/V projections",
          "l2 q/k normalization",
          "sigmoid beta projection",
          "output RMSNorm",
          "output projection"
        ],
        "delta": true,
        "gate_scope": "none",
        "read_timing": "after_update",
        "scale_rule": "key_dim_rsqrt"
      },
      "state": {"initial_state": "zero (cache modes are residual)"},
      "cache": {}
    }
  ],
  "coverage": {
    "level": "generic_model_integration",
    "validated": true,
    "notes": "Mixer parity vs the pinned fused_recurrent_delta_rule (GPU) and full-layer composition in tests/test_architectures_deltanet.py. The short convolution, chunked prefill path, cache/decode modes and use_gate/o_norm-gated variants are residual external work, not claimed."
  }
}

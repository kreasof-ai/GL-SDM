{
  "schema_version": 1,
  "name": "kda",
  "kind": "complete_model_graph",
  "source": {
    "comparator": "benchmarks.comparators.fla_k2",
    "notes": "arch-028. U2.D + key-channel decay: S ← S·exp(g_t) (g ∈ R^K per channel), S ← S + (β_t k_t) ⊗ (v_t − k_tᵀ S_decayed), after-update read with explicit 1/√K read-scale. Verified against fla/ops/kda/naive.py + fla/layers/kda.py @ 864a87f6. External stages: Q/K/V projections (silu), gate projection + A_log/dt_bias schedule, beta projection, output projection. Layer module: architectures/kda.py."
  },
  "layers": [
    {
      "id": "kda_layer",
      "operation": "linear_delta_state",
      "external_component": "architectures.kda.KDALayer",
      "params": {
        "external_stages": ["Q/K/V projections (silu)", "gate projection + A_log/dt_bias schedule (per-channel)", "beta projection", "output projection"],
        "delta": true,
        "gate_scope": "channel",
        "read_timing": "after_update",
        "scale_rule": "key_dim_rsqrt"
      },
      "state": {"initial_state": "zero (cache modes are residual)"},
      "cache": {}
    }
  ],
  "coverage": {
    "level": "generic_model_integration",
    "validated": true,
    "notes": "Mixer parity vs the pinned fused_recurrent_kda (GPU) and full-layer composition in tests/test_architectures_kda.py. The in-kernel qk-l2norm / gate / beta-sigmoid flags, GVA (num_v_heads ≠ num_heads), allow_neg_eigval, lower_bound and cache/chunked paths are residual external work, not claimed."
  }
}
